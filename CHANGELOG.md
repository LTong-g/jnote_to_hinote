# 更新日志

本文件记录本项目的所有重要变更。

## [未发布]

### 工程维护

- 将当前转换逻辑收敛为 `converter.py`、`reader.py` 和 `thumbnail.py`，移除运行时历史版本继承链。
- 删除历史源码哈希冻结机制；历史发布继续由 Git 提交和标签保存。
- 将分散的版本验证文档合并为 `docs/validation.md`，删除空示例目录和过时检查脚本。
- CI 仅在 Python 3.10 与 3.13 上运行当前代码检查和测试；Windows 打包改为标签或手动触发，并上传构建产物。
- 清理转换器中的旧参考模板实现并统一项目版本字段；缩略图测试合并为单一测试模块。
- Windows 构建只携带 x64 TkDND 运行库并排除不支持的加密 PDF 依赖；版本标签会发布带 ZIP 安装包的 GitHub Release。

## [1.6.0] - 2026-09-05

安全输出、批量报告和工程维护版本。

### 修复

- 单文件输出路径未以 `.hinote` 结尾时自动追加该扩展名；例如 `note` 生成 `note.hinote`，`note.txt` 生成 `note.txt.hinote`，避免误将源 `.Jnotes` 路径作为输出覆盖。
- 单文件转换错误默认改为简洁错误信息；`--debug` 可恢复完整异常信息。
- 批量命令在部分失败时返回退出码 2，不再错误地报告完全成功。
- 批量汇总分别记录转换失败、跳过、输入错误和未执行数量，修复 GUI 取消任务时的剩余数量计算。

### 新增

- 增加匿名化 JSON 报告选项，移除笔记标题与完整路径。
- 增加全部冻结核心的 SHA-256 清单和自动校验。
- CI 增加 Ruff、wheel 构建及 Windows 打包自检。

### 依赖与构建

- 使用维护中的 `pypdf` 代替已弃用的 `PyPDF2`。
- 将经过现有 Windows 构建路径验证的 PyInstaller 最低版本设为 5.13，并在打包前校验完整依赖。

### 版本管理

- 新增 v1.6.0 安全输出包装核心；v1.5.3 及更早转换核心保持冻结。

## [1.5.3] - 2026-09-04

横向 PDF 缩略图兼容修复版本。

### 修复

- 按原生 Hinote 规则将横向页写为 `pageRatio<1` 且 `pageOrientation=1`，并同步顶层笔记方向；修复之前的 `pageRatio>1` 且 `pageOrientation=0` 结构被华为笔记拒绝解析的问题。
- 缩略图由“高度固定 1080”改为“最长边固定 1080”；例如原生横向页的 `pageRatio=0.625`、`pageOrientation=1` 对应 1080×675 缩略图。
- PDF/outline 顶层资源先于页面资源写入，`custom_md.jhinote` 与物理资源顺序保持一致。
- 增加横向 4:3、1.414:1 页面、方向元数据和 PDF 封包顺序回归测试。

### 版本管理

- 新增 v1.5.3 转换核心，v1.5.2 及更早的版本化核心保持不变。

## [1.5.2] - 2026-09-03

缩略图生成修复版本。

### 修复

- 新增 v1.5.2 转换核心，在保留 v1.5.1 已冻结的可编辑内容输出后重建缩略图。
- 普通笔记缩略图统一按页面比例生成 1080 像素高 JPEG，使用质量 100 和原生样本一致的 4:2:0 采样，消除原先 300 像素预览被放大后的明显模糊。
- PDF 笔记为每一个 PDF 背景页生成缩略图，不再仅依赖源封面或让后续页缩略图为空白；预览同时包含 PDF 正文与笔迹、图片、文本等覆盖层。
- PDF 与普通页面混排时，按每页的 PDF 附件索引渲染对应背景，并将 PDF 笔记页面的 `cloudSyncState` 写为与原生 PDF 样本一致的 `0`。

### 依赖与验证

- 新增 `pypdfium2`，用于可靠地栅格化嵌入的原始 PDF 页面。
- 增加普通页、PDF 多页和 PDF/普通页面混排的缩略图结构、尺寸、JPEG 参数及像素内容回归测试。

### 版本管理

- 项目版本和当前转换核心升至 v1.5.2；v1.5.1 及更早的版本化核心保持不变。

## [1.5.1] - 2026-09-02

Jnotes 容器兼容性修复版本。

### 修复

- 新增 v1.5.1 转换核心，兼容外层 ZIP 数据入口名为 `zip.Jzip` 或 `zip.Jnotes` 的 Jnotes 文件。
- 普通笔记和 PDF 笔记统一使用同一次解析结果，避免 `zip.Jnotes` 文件在普通笔记分支中再次被旧解析器拒绝。
- 对缺少入口、入口重复、入口歧义、内层版本不支持和记录截断等情况给出明确错误，不再静默截断损坏记录。

### 版本管理

- 项目版本升至 v1.5.1。
- 当前转换核心升至 v1.5.1，与发生本次核心修改的项目版本一致；v1.2.0 及更早版本化核心保持不变。
- 从本版本起，核心发生修改时使用对应的项目版本号；未修改核心时继续引用最近一次核心版本。
- Jnotes 内层协议仍为 TRY v2，Hinote 输出结构未改变。

## [1.5.0] - 2026-09-02

桌面界面布局和输入体验改进版本。

### 新增

- 输入列表支持将 `.Jnotes`、`.jnote`、文件夹和 TXT 清单直接拖入。
- 增加拖放路径解析测试，并为 Windows PyInstaller 打包收集 TkDND 运行资源。

### 改进

- GUI 改为左右两列布局，左侧管理输入/输出，右侧显示结果/日志。
- 输入列表、结果列表和运行日志获得更充足的可伸缩空间。
- 增加列表横向滚动条，并允许拖动左右及结果/日志分隔条。

### 版本管理

- 项目版本升至 v1.5.0。
- 转换核心继续使用 v1.2.0，未修改历史转换行为。

## [1.4.0] - 2026-09-01

原生 PDF 页面转换版本。

### 新增

- 新增 `converter_v1_2_0.py`，保留 Jnotes 中的原始 PDF 文件。
- 使用 Hinote 顶层 `attachType=3` PDF 附件和页面 `bkgAttachmentId` / `bkgAttachmentIndex` 引用关系。
- PDF 页面上的笔迹、图片和文本继续保持为独立的可编辑覆盖层。
- 按源页面宽高保存页面比例，支持同一本 PDF 中页面比例不同的情况。
- 增加 PDF 页数、页码引用、原始字节哈希和输出结构验证。

### 修复

- 修复 PDF-backed Jnotes 转换后只有少数笔迹页可见、其他页面为空白的问题。
- 修复将低分辨率 Jnotes 封面缩略图放大为整页导致封面模糊的问题。

### 版本管理

- 项目版本升至 v1.4.0。
- 新增 v1.2.0 转换核心，保留 v1.1.2 及更早核心不变。

## [1.3.1] - 2026-09-01

中文文本缩略图兼容性修复版本。

### 修复

- 新增 `converter_v1_1_2.py`，作为当前转换核心。
- 缩略图改用计算机中支持中文和英文的系统字体，修复中文文本触发 `latin-1` 编码错误导致转换失败的问题。
- 保留 `converter_v1_1_1.py` 不变，作为历史 v1.1.1 核心。

### 版本管理

- 项目版本升至 v1.3.1。
- v1.1.2 继承 v1.1.1 的文件格式和笔迹映射行为，仅修复缩略图文字绘制兼容性。

## [1.3.0] - 2026-08-26

桌面图形界面版本。

### 新增

- 增加中文优先的 Tkinter 桌面 GUI，可通过 `jnotes2hinote-gui` 启动。
- 支持添加多个 Jnotes 文件、文件夹和 TXT 路径清单。
- 支持递归扫描、输出重名策略、JSON 报告、实时进度、运行日志和安全停止。
- 支持中英文界面切换、最近使用设置记忆和转换完成后打开输出目录。
- 增加 `.[build]` 可选依赖和 Windows PyInstaller 打包脚本。

### 兼容性

- 命令行接口和 v1.2.0 的批量转换行为保持兼容。
- `.hinote` 文件转换仍使用 v1.1.1 自包含核心；本版本没有修改转换核心，因此 `CORE_SHA256.txt` 不变。

## [1.2.0] - 2026-08-25

批量转换功能版本。

### 新增

- 支持输入单个目录，默认转换目录直接子级中的 `.Jnotes`/`.jnote` 文件。
- 支持通过 `--recursive` 递归转换目录及其所有子目录中的文件。
- 支持一次输入多个文件或目录。
- 支持输入 TXT 路径清单，每行读取一个路径；清单中的相对路径以清单文件所在目录为基准。
- 批量转换时自动跳过错误路径，并在终端和 JSON 汇总报告中记录错误。
- 增加 `jnotes2hinote --version` 命令。

### 兼容性

- 单文件转换命令保持不变。
- `.hinote` 文件格式转换仍使用当前的 v1.1.1 自包含核心；本版本主要扩展命令行输入和批量处理能力。

## [1.1.1] - 2026-08-22

几何宽度映射修复版本。

### 修复

- 移除普通不透明 Jnotes type 6/type 7 几何图形的错误固定宽度档位。
- 几何宽度改为与普通笔迹相同的直接映射：Jnotes `d` 原值写入 Huawei PENCILENGINE 的 `style+84`。
- 保留 `3.04`、`4.93`、`12.72` 等带小数的源几何宽度，不再强制转换为固定档位。
- 保持经过设备验证的半透明 `type=6, b=12` 荧光笔生成曲线规则不变。

### 版本管理

- 新增 `converter_v1_1_1.py`，作为当前转换核心。
- 保留 `converter_v1_1_0.py`，作为历史 v1.1.0 核心。
- 保持冻结的 `converter_v1_0_0.py` 和 `CORE_SHA256.txt` 不变。

### 验证

- 自动化测试通过。
- 171 页笔记重新转换和 PENCILENGINE 结构解析通过，涉及 44,828 条记录。
- 本版本继承此前已完成的自包含 PENCILENGINE 结构验证；本版本新增的几何宽度映射仍应单独在设备上复测。

## [1.1.0] - 2026-08-22

自包含 PENCILENGINE 生成版本。

### 变更

- 不再要求用户提供华为 `.hinote` 参考文件。
- 由转换器直接生成华为笔记所需的原生笔迹和几何图形数据。
- 普通转换命令简化为 `jnotes2hinote input.Jnotes output.hinote`。
- 保持 `converter_v1_0_0.py` 不变，作为冻结的旧版参考文件实现。

### 修复

- 恢复经过设备验证的几何宽度行为，修复 v1.0.0 的宽度换算回归。
- 将半透明平滑曲线正确转换为华为原生荧光笔笔迹，并保留源透明度。
- 修复验证过程中发现的宽度/透明度回归，并保持其他几何行为稳定。

### 验证

已在一份 171 页的笔记上完成完整设备验证，包含 44,828 条笔迹、48 张图片/贴纸、35 个文本框和 245 条 type 6/type 7 几何记录。

验证版本：

- Jideos 云记 **3.2.3.2**
- 华为笔记 **15.0.14.295**

## [1.0.0] - 2026-08-21

首个经过设备验证的公开版本。

### 验证环境

- 源应用：**Jideos 云记 3.2.3.2**
- 目标应用：**华为笔记 15.0.14.295**
- 验证方式：将生成的 `.hinote` 文件导入华为设备并检查。

### v1.0.0 已支持

- 多页 `.Jnotes` → `.hinote`
- 原生可编辑的 PENCILENGINE 手写笔迹
- 圆珠笔、钢笔、HB 铅笔和荧光笔映射
- 华为笔记 15.0.14.295 使用的 BGR 颜色转换
- 经过设备标定的荧光笔宽度以及渲染/界面透明度
- 测试笔记中涉及的 Jnotes 几何图形
- 图片和图片型贴纸
- 基本的可编辑文本框
- 封面图片迁移
- 华为原生空白/横线/点阵/方格纸张模板
- SHA-256 元数据重建（`fileMdStr`、`fileNameMdStr`、`detailFileMap`）

### 已知限制

- 尚未确认纸胶带的华为原生等价物，冻结的 v1.0.0 核心会跳过它。
- 音频格式已经逆向确认，但 v1.0.0 尚未启用音频转换。
- 文本的字体效果、行距和换行可能不同。
- 普通笔类的视觉笔宽尚未在所有笔型上完成完整标定。
- 除已验证的版本组合外，其他应用版本的兼容性尚未验证。
